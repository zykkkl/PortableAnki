# Portable Anki 墨水屏设备第一版制作方案

## 1. 项目定位

制作一个便携式 Anki 复习终端：

```text
电脑 Anki/服务器 + AnkiConnect
        ↓ Wi-Fi 下载今日卡片
ESP32 便携设备
        ↓ 离线复习、按键评分
重新连网
        ↓ Wi-Fi 上传评分
电脑 Anki/服务器 写入复习结果并同步
```

设备端负责：

- 显示卡片正面和背面
- 接收实体按键输入
- 记录 Again / Hard / Good / Easy 评分
- 通过 Wi-Fi 传输数据

## 2. 总体架构

```text
┌──────────────────────────┐
│   电脑端 Anki / 服务器     │
│  Anki + AnkiConnect 插件  │
└─────────────┬────────────┘
              │ HTTP
┌─────────────▼────────────┐
│     Python 桥接程序       │
│ 导出今日卡片 / 写入评分   │
└─────────────┬────────────┘
              │ Wi-Fi HTTP
┌─────────────▼────────────┐
│        ESP32 设备         │
│ 卡片显示 / 按键 / 本地存储 │
└─────────────┬────────────┘
              │ SPI
┌─────────────▼────────────┐
│       三色墨水屏模块       │
└──────────────────────────┘
```

## 3. 硬件清单

### 3.1 推荐配置

| 模块 | 建议 |
|---|---|
| 主控 | ESP32-S3 DevKitC-1 或普通 ESP32 开发板 |
| 屏幕 | 2.9 寸三色墨水屏模块，优先选择带驱动板的模块 |
| 按键 | 6 个轻触按键 |
| 电池 | 3.7V 单节锂电，1000-2000mAh |
| 电源模块 | 1S 锂电充电保护 + 5V 升压模块，输出建议不低于 1A |
| 开关 | 拨动电源开关 |
| 连接材料 | 洞洞板、排针、硅胶线、JST 插座、杜邦线 |
| 外壳 | 3D 打印外壳，M2 螺丝柱固定 |

### 3.2 屏幕选择说明

黑白双色墨水屏，因为黑白屏通常刷新更快，并且部分型号支持局部刷新。

如果坚持使用三色墨水屏，但要接受这些限制：

- 翻页慢
- 一般不支持局部刷新
- 红色刷新更慢
- 不适合高频快速复习

三色屏适合学习硬件驱动和制作原型，但如果目标是每天大量刷 Anki，黑白屏体验更好。

## 4. 供电方案

### 4.1 调试阶段

调试时优先使用 USB 供电：

```text
电脑 USB / 充电宝
        ↓
ESP32 USB 口
        ↓
墨水屏模块
```

优点：

- 安全
- 接线简单
- 方便串口调试
- 不需要一开始处理电池和充电问题

### 4.2 便携阶段

装入外壳时使用内置锂电：

```text
3.7V 锂电
   ↓
充电保护模块
   ↓
5V 升压模块
   ↓
ESP32 5V/VIN 或 USB 输入
   ↓
墨水屏模块
```

注意事项：

- 不要把单节锂电直接接到 ESP32 的 5V/VIN。
- 不要使用纽扣电池，ESP32 开 Wi-Fi 时电流峰值较高。
- 升压模块输出电流建议至少 1A。
- 锂电软包不能被螺丝、焊点、尖角挤压。
- 外壳内建议给电池单独留电池仓。
- 充电口、电源开关和复位按键需要从外壳露出。

## 5. 按键设计

### 5.1 按键数量

第一版建议 6 个按键：

| 按键 | 功能 |
|---|---|
| 翻面 | 正面/背面切换 |
| Again | 评分 1 |
| Hard | 评分 2 |
| Good | 评分 3 |
| Easy | 评分 4 |
| 同步/返回 | 下载卡片、上传记录或返回列表 |

### 5.2 推荐布局

```text
┌────────────────────────┐
│        墨水屏          │
├────────────────────────┤
│ 翻面 │ Again │ Hard │
│ Good │ Easy  │ 同步 │
└────────────────────────┘
```

### 5.3 按键接线

每个按键一端接 GPIO，另一端接 GND。

ESP32 程序中 GPIO 使用 `INPUT_PULLUP`：

```cpp
pinMode(BUTTON_GOOD, INPUT_PULLUP);
```

按下时读取到 `LOW`，松开时读取到 `HIGH`。

## 6. 接线示例

以下 GPIO 只是示例，实际应按开发板和屏幕模块说明调整：

```text
墨水屏 VCC  → 3V3 或 5V，按模块说明
墨水屏 GND  → GND
墨水屏 DIN  → GPIO11
墨水屏 CLK  → GPIO12
墨水屏 CS   → GPIO10
墨水屏 DC   → GPIO13
墨水屏 RST  → GPIO14
墨水屏 BUSY → GPIO9

翻面按键    → GPIO4
Again 按键  → GPIO5
Hard 按键   → GPIO6
Good 按键   → GPIO7
Easy 按键   → GPIO15
同步按键    → GPIO16
```

建议：

- 避开 `GPIO0` 等启动相关脚位。
- 如果接线后 ESP32 无法启动，优先更换按键 GPIO。
- 屏幕和 ESP32 必须共地。
- 先用杜邦线验证，再焊接到洞洞板。

## 7. 软件方案

### 7.1 设备端固件

建议使用 Arduino IDE 或 PlatformIO。

推荐库：

| 功能 | 库 |
|---|---|
| 墨水屏驱动 | GxEPD2 |
| JSON 解析 | ArduinoJson |
| Wi-Fi | ESP32 WiFi |
| HTTP 通信 | HTTPClient |
| 本地存储 | LittleFS |

设备端主要模块：

- 屏幕显示模块
- 按键扫描模块
- 卡片状态机
- 本地 JSON 存储
- Wi-Fi 同步模块
- 复习日志模块

### 7.2 电脑端桥接程序

电脑端建议使用 Python。

主要功能：

- 连接 AnkiConnect
- 获取今日待复习卡片
- 生成设备可读的 `today.json`
- 启动本地 HTTP 服务
- 接收设备上传的 `review_log.jsonl`
- 调用 AnkiConnect 将评分写回 Anki

### 7.3 AnkiConnect

AnkiConnect 是 Anki 插件，可以让外部程序通过 HTTP API 操作 Anki。

第一版中，设备不直接接触 Anki 数据库，而是通过电脑端 Python 程序间接操作 Anki。

## 8. 数据格式

### 8.1 今日卡片文件

设备下载的 `today.json`：

```json
{
  "deck": "English",
  "generatedAt": "2026-06-05T10:00:00+08:00",
  "cards": [
    {
      "cardId": 1498938915662,
      "front": "abandon",
      "back": "v. 放弃；遗弃"
    },
    {
      "cardId": 1498938915663,
      "front": "brief",
      "back": "adj. 简短的；n. 摘要"
    }
  ]
}
```

### 8.2 复习记录文件

设备保存的 `review_log.jsonl`：

```json
{"cardId":1498938915662,"ease":3,"timeMs":8200}
{"cardId":1498938915663,"ease":1,"timeMs":15500}
```

字段说明：

| 字段 | 含义 |
|---|---|
| cardId | Anki 卡片 ID |
| ease | 评分，1=Again，2=Hard，3=Good，4=Easy |
| timeMs | 本张卡从显示正面到评分的耗时 |

## 9. 设备端交互流程

```text
开机
 ↓
显示设备状态
 ↓
按同步键
 ↓
连接 Wi-Fi，下载 today.json
 ↓
显示第一张卡正面
 ↓
按翻面键
 ↓
显示背面
 ↓
按 Again / Hard / Good / Easy
 ↓
保存评分，进入下一张
 ↓
复习完成
 ↓
按同步键上传 review_log.jsonl
```

## 10. 外壳方案

### 10.1 结构

建议外壳分为三层：

```text
上盖：屏幕开窗 + 按键孔
中框：固定 ESP32、屏幕模块、洞洞板
下盖：电池仓 + 螺丝柱
```

### 10.2 开孔

需要预留：

- 屏幕窗口
- 6 个按键孔
- USB-C 充电/调试口
- 电源拨动开关
- 复位孔，可选
- 挂绳孔，可选

### 10.3 尺寸建议

第一版可以按较宽松尺寸设计：

```text
长：110mm
宽：60mm
厚：18mm
```

实际尺寸应根据屏幕模块、ESP32 开发板、电池尺寸调整。

### 10.4 固定方式

- 屏幕使用泡棉胶或薄双面胶固定。
- ESP32 和洞洞板用 M2 螺丝柱固定。
- 电池用泡棉胶固定在独立电池仓。
- 不要让外壳直接压迫墨水屏玻璃。
- 不要让螺丝柱、焊点、排针顶到锂电池。

## 11. 开发步骤

### 阶段 1：屏幕点亮

目标：

- ESP32 通过 USB 供电
- 墨水屏显示 `Hello Anki`

完成标准：

- 屏幕能稳定刷新
- 断电后画面保留
- 串口无明显错误

### 阶段 2：按键验证

目标：

- 接入 6 个按键
- 串口打印每个按键事件

完成标准：

- 每个按键都能被识别
- 没有明显误触发
- 简单消抖可用

### 阶段 3：本地假卡复习

目标：

- 固件内写死 3-5 张假卡
- 实现正面、背面、评分、下一张

完成标准：

- 能完成一轮复习
- 评分能保存到内存或文件

### 阶段 4：本地 JSON

目标：

- 使用 LittleFS 保存 `today.json`
- 从 JSON 读取卡片
- 将评分写入 `review_log.jsonl`

完成标准：

- 断电后数据不丢失
- 能继续未完成的复习

### 阶段 5：Wi-Fi 下载卡片

目标：

- ESP32 连接 Wi-Fi
- 从电脑端 HTTP 服务下载 `today.json`

完成标准：

- 按同步键能下载卡片
- 下载失败时屏幕显示错误状态

### 阶段 6：电脑端 Anki 桥接

目标：

- Python 程序连接 AnkiConnect
- 生成今日卡片 JSON
- 接收设备上传的复习记录

完成标准：

- 电脑端能看到设备请求
- 能生成合法 `today.json`
- 能接收 `review_log.jsonl`

### 阶段 7：写回复习结果

目标：

- 将设备上传的评分写回 Anki

完成标准：

- Anki 中对应卡片的复习状态发生变化
- 再次导出今日卡片时，已复习卡片不重复出现

### 阶段 8：便携供电

目标：

- 加入锂电、充电保护、升压模块、电源开关

完成标准：

- 设备可以脱离 USB 工作
- Wi-Fi 同步时不重启
- 充电和开关正常

### 阶段 9：焊接与外壳

目标：

- 从面包板迁移到洞洞板或模块固定结构
- 装入 3D 打印外壳

完成标准：

- 摇晃设备不会断线
- 按键手感稳定
- USB 口和开关可正常使用

## 12. 第一版功能边界

第一版建议暂不支持：

- 音频播放
- 图片显示
- 富文本 HTML
- LaTeX
- 多牌组复杂筛选
- 设备端编辑卡片
- 设备端新建卡片
- 直接同步 AnkiWeb
- 完整 Anki 调度算法

第一版优先支持：

- 文本正反面
- 一个默认牌组或默认筛选条件
- 离线复习
- 评分记录
- Wi-Fi 同步到电脑端

## 13. 主要风险

| 风险 | 说明 | 规避方式 |
|---|---|---|
| 三色屏刷新慢 | 高频翻卡体验差 | 接受原型限制，后续换黑白屏 |
| Anki 写回复杂 | 直接操作数据库风险高 | 通过 AnkiConnect 桥接 |
| 电池供电不稳 | Wi-Fi 峰值电流导致重启 | 选择足够电流的升压模块 |
| 外壳挤压屏幕 | 墨水屏易损坏 | 屏幕周围留缓冲，不直接压玻璃 |
| 卡片格式复杂 | HTML、图片、音频难适配 | 第一版只支持纯文本 |

## 14. 最终第一版配置

```text
ESP32-S3 开发板
+ 2.9 寸三色墨水屏模块
+ 6 个实体按键
+ 2000mAh 单节锂电
+ 1S 充电保护升压模块
+ 洞洞板焊接
+ 3D 打印外壳
+ Python + AnkiConnect 桥接程序
```

如果实际复习体验优先，建议将三色墨水屏替换为黑白双色墨水屏。

## 15. 后续升级方向

第一版完成后，可以逐步升级：

- 换黑白局刷墨水屏，提高翻页速度
- 增加休眠和低功耗管理
- 增加电量显示
- 增加多牌组选择
- 增加图片压缩显示
- 增加音频播放模块
- 改为定制 PCB
- 优化外壳尺寸和按键手感
- 做一个更完善的桌面端同步工具
